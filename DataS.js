const { nativeTouchData } = require("react-dom/test-utils");

const expDiv = (x, y, p ) => {
    if (p == 1) return 0;
    result = x
    for(let i = 1; i < y; i++ ){
        result*= x 
    }
        return result % p
}


const expWhile = (x, y , p) => {
    result = x
    while(y - 1 > 0){
        result *= x
        y--
    }
    return result % 6
}


var twoSum = function(nums, target) {
    for(let i = 0 ; i < nums.length ; i++){
        var requiredNumber = target - nums[i]
        
        }
        s
return [x, y]

};
console.log(twoSum([3, 2, 3], 6))

